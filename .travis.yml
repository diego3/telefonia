language: php
php:
- 5.6
- 5.5

before_install:
  - mysql -e 'CREATE DATABASE telefonia;'
  
before_script:
  - composer self-update
  - composer install --no-interaction --prefer-source --dev

services:
  - mysql

script: phpunit app/api/tests